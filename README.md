# First Agent

A local CLI AI system controller using the OpenAI API. It can list files, read/write/replace files, run safe shell commands, and get the local time.

## Setup

1. Create and activate a virtual environment
2. Install dependencies
3. Add `OPENAI_API_KEY` to `.env`
4. Run:

```bash
python agent.py
```